apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api
  namespace: swarmdev
  labels:
    app.kubernetes.io/name: api
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: api
