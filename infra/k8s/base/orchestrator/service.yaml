apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: swarmdev
  labels:
    app.kubernetes.io/name: orchestrator
    app.kubernetes.io/component: orchestration
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: orchestrator
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
