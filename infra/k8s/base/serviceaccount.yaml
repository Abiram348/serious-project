apiVersion: v1
kind: ServiceAccount
metadata:
  name: swarmdev
  namespace: swarmdev
  labels:
    app.kubernetes.io/name: swarmdev
    app.kubernetes.io/component: service-account
  annotations:
    # For production, use IRSA (IAM Roles for Service Accounts)
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/swarmdev-pod-role
